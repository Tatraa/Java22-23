import org.junit.Test;

import static org.junit.Assert.*;

public class ProductTests {

    @Test
    void testApp() {
        Boolean True = Boolean.TRUE;
        assertEquals(True, True);
    }
    //nei rozumiem jak mamy wczytywac dane z podanej tabelki skoro nie bylo zadnego pliku excelowskiego albo SQLowskiego
}